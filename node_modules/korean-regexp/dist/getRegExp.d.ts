interface GetRegExpOptions {
    initialSearch?: boolean;
    startsWith?: boolean;
    endsWith?: boolean;
    ignoreSpace?: boolean;
    ignoreCase?: boolean;
    global?: boolean;
    fuzzy?: boolean;
    nonCaptureGroup?: boolean;
    engToKor?: boolean;
}
declare const getRegExp: (search: string, { initialSearch, startsWith, endsWith, ignoreSpace, ignoreCase, global, fuzzy, nonCaptureGroup, engToKor: _engToKor, }?: GetRegExpOptions) => RegExp;
export default getRegExp;
